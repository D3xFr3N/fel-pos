# Modo FEL: disabled (solo ticket POS) | demo | production
FEL_MODE=demo
# Perfil del negocio: abarrotes | farmacia | libreria | ferreteria | restaurante | boutique
BUSINESS_PROFILE=abarrotes
SECURITY_SECRET=CAMBIA_ESTE_SECRETO
ACCESS_TOKEN_MINUTES=720

# Datos persistentes (NO borrar al actualizar)
# Base de datos y respaldos viven en ./data
DATABASE_URL=sqlite:///./data/fel_pos.db
BACKUP_DIR=./data/backups
# Opcional: carpeta personalizada de datos
# FELPOS_DATA_DIR=./data

# Datos del emisor (tu empresa)
EMISOR_NIT=12345678
EMISOR_NOMBRE=Mi Empresa S.A.
EMISOR_NOMBRE_COMERCIAL=Mi Tienda
EMISOR_DIRECCION=Ciudad de Guatemala
EMISOR_CODIGO_POSTAL=01001
EMISOR_MUNICIPIO=Guatemala
EMISOR_DEPARTAMENTO=Guatemala
EMISOR_PAIS=GT
EMISOR_AFILIACION_IVA=GEN
EMISOR_ESTABLECIMIENTO=1

# Certificador (Infile, Digifact, etc.) — solo en modo produccion
CERTIFICADOR=infile
CERTIFICADOR_USUARIO=
CERTIFICADOR_LLAVE=
CERTIFICADOR_URL=https://certificador.infile.com/api

# Gmail (opcional, para enviar ordenes por correo)
GMAIL_SENDER=
GMAIL_APP_PASSWORD=
GMAIL_SMTP_HOST=smtp.gmail.com
GMAIL_SMTP_PORT=587

# WhatsApp API Cloud (opcional, para envio real)
WHATSAPP_PHONE_ID=
WHATSAPP_TOKEN=
WHATSAPP_API_URL=https://graph.facebook.com/v20.0

# Consulta automatica de NIT (opcional)
# Ejemplo de uso: NIT_LOOKUP_URL=https://tu-proveedor/api/nit
# El sistema consultara: {NIT_LOOKUP_URL}/{NIT}
NIT_LOOKUP_URL=
NIT_LOOKUP_TOKEN=
NIT_LOOKUP_TIMEOUT_SECONDS=8

# Impresion ticket (Windows + impresora termica ESC/POS)
# Si RECEIPT_PRINTER_NAME va vacio, usa la impresora predeterminada de Windows.
RECEIPT_PRINTER_NAME=
RECEIPT_PRINT_ON_CHECKOUT=true
RECEIPT_OPEN_DRAWER_ON_CHECKOUT=true
RECEIPT_CHARS_PER_LINE=48
RECEIPT_BOTTOM_FEED_LINES=8
RECEIPT_ENCODING=cp850
# Personalizacion del ticket (deja vacio encabezado para usar datos de empresa)
RECEIPT_HEADER_LINE_1=
RECEIPT_HEADER_LINE_2=
RECEIPT_HEADER_LINE_3=
RECEIPT_SHOW_COMPANY_NIT=true
RECEIPT_SHOW_ADDRESS=false
RECEIPT_CENTER_HEADER=false
RECEIPT_FOOTER_MESSAGE=Gracias por su compra
RECEIPT_FOOTER_EXTRA=
RECEIPT_TICKET_LABEL=TICKET #{id}
RECEIPT_SEPARATOR_CHAR=-
RECEIPT_SHOW_CUSTOMER=true
RECEIPT_SHOW_DATE=true
RECEIPT_SHOW_SUBTOTAL=true
RECEIPT_SHOW_TAX=true
RECEIPT_SHOW_PAYMENTS=true
RECEIPT_SHOW_FEL=true
RECEIPT_SHOW_WHOLESALE_SAVINGS=true
RECEIPT_SHOW_ITEM_DETAIL=true

# Impresion de etiquetas (codigo de barras)
# Si LABEL_PRINTER_NAME va vacio, usa RECEIPT_PRINTER_NAME o impresora predeterminada.
LABEL_PRINTER_NAME=
LABEL_DEFAULT_WIDTH_MM=50
LABEL_DEFAULT_HEIGHT_MM=30
LABEL_ENCODING=cp850

# Respaldos de base de datos SQLite
BACKUP_DIR=./data/backups
BACKUP_AUTO_DAILY=true
BACKUP_RETENTION_DAYS=30
BACKUP_AUTO_ON_COMMIT=true
BACKUP_AUTO_MIN_INTERVAL_SECONDS=60

# Actualizaciones automaticas (GitHub Pages)
# Cambia solo si publicas actualizaciones en otro servidor.
UPDATE_MANIFEST_URL=https://D3xFr3N.github.io/fel-pos/latest.json

# Red local / APK movil
# 0.0.0.0 = permite celular en la misma WiFi (recomendado)
# 127.0.0.1 = solo este PC
FELPOS_BIND_HOST=0.0.0.0
FELPOS_PORT=8000

# Licencia por tienda (firmada, validacion local privada)
STORE_LICENSE_KEY=
LICENSE_REQUIRED_FOR_UPDATES=true
# LICENSE_REGISTRY_URL=  # opcional, solo licencias antiguas con registro remoto

# Deprecado: cada cajero abre su propio fondo. Esta variable ya no se usa.
# CASH_SHARED_SESSION=true

# Zona horaria para fechas en ticket impreso (Guatemala)
APP_TIMEZONE=America/Guatemala

# Puente scanner para APK movil (modo Bluetooth/TCP)
SCANNER_BRIDGE_ENABLED=false
SCANNER_BRIDGE_PORT=18765
SCANNER_BRIDGE_USERNAME=admin
SCANNER_BRIDGE_PASSWORD=
# SCANNER_BRIDGE_COM_PORT=COM5
